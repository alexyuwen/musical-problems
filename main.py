def func(n, k):
    # Let notes be a sorted set of size n.  Let 0 <= k <= n - 1 be the start index.
    # Assume a function, dist(i, j), that returns the distance between buttons i and j in O(1) time.  Note 0 <= i, j <= n - 1
    # XXX

    # DYNAMIC PROGRAMMING OR BACKTRACKING?


    return
